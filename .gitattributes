# Normalize line endings automatically
* text=auto

# Shell scripts and Makefiles always use LF (safe even when cloned on Windows)
*.sh     text eol=lf
Makefile text eol=lf

# Windows scripts use CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf
*.cmd   text eol=crlf
